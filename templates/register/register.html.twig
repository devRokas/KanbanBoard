{% extends 'base.html.twig' %}

{% block body %}
    <div>
        <div class="row">
            <div class="col-md-2 offset-md-5 login_form">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}